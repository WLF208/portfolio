# 作品集 GitHub 发布教程

这份教程只针对你的作品集，按步骤操作就可以自己维护。

## 1. 当前已经完成的部分

- 本地文件夹：`C:\Users\lufei\Desktop\面试复盘\作品集`
- GitHub 仓库：`https://github.com/WLF208/portfolio`
- 在线作品集：`https://wlf208.github.io/portfolio/`
- 本地到 GitHub 的连接已经配好，后面只需要“改内容 -> 提交 -> 推送”

## 2. 一次完整的更新流程

1. 往 `images` 文件夹放图片，或修改 `index.html`
2. 双击 `index.html` 本地预览
3. 打开 PowerShell，进入作品集目录
4. 执行 `git add .`
5. 执行 `git commit -m "更新说明"`
6. 执行 `git push`
7. 等 1-2 分钟，打开 `https://wlf208.github.io/portfolio/` 检查

## 3. 添加真实项目图片

把照片或视频截图复制到：

```text
C:\Users\lufei\Desktop\面试复盘\作品集\images\
```

例如图片叫 `car.jpg`。

用记事本或 VSCode 打开 `index.html`，找到某个项目里的：

```html
<div class="media-frame">
  <svg>...原来的示意图...</svg>
</div>
```

把整段 `<svg>...</svg>` 替换成：

```html
<img src="images/car.jpg" alt="平衡车实物">
```

注意三点：

- 路径是 `images/图片名`，不是 `C:\...`
- 图片名要和文件名完全一致
- 页面已经写好了图片自适应样式，正常尺寸的横图都能铺满

## 4. 修改联系信息和项目链接

用 VSCode 打开 `index.html`，直接搜索下面这些中文或 `#`：

- 顶部电话、邮箱、城市
- 项目里的“项目仓库”“演示视频”“设计文档”按钮
- 每个项目简介和指标

把 `href="#"` 改成真实地址，例如：

```html
<a class="btn primary" href="https://github.com/WLF208/portfolio">项目仓库</a>
```

保存后先双击 `index.html` 预览，确认没问题再提交推送。

## 5. 提交和推送的命令解释

```powershell
cd C:\Users\lufei\Desktop\面试复盘\作品集
git status
```

`git status` 查看当前改了哪些文件，绿色文件表示已经加入，红色表示还没加入。

```powershell
git add .
```

把 `作品集` 文件夹里所有改动都加入本次提交。

```powershell
git commit -m "更新作品集图片和链接"
```

给这次修改拍一张快照，引号里写清楚改了什么。

```powershell
git push
```

把本地快照上传到 GitHub。上传成功后，GitHub Pages 会自动重新生成网页。

## 6. 如果换电脑或者本地文件夹丢了

先安装 Git，然后打开 PowerShell 执行：

```powershell
git clone https://github.com/WLF208/portfolio.git
cd portfolio
```

之后继续用 `git add .`、`git commit -m "..."`、`git push` 这套流程。

## 7. 常见问题

### 图片显示不出来

- 检查图片是否真的在 `images` 文件夹里
- 检查 `index.html` 里的路径是否写成 `images/图片名`
- 检查图片名的大小写是否和文件名一致
- 本地预览正常但网页上没更新，按 `Ctrl+F5` 强制刷新

### 网页还是旧版

GitHub Pages 生成需要 1-2 分钟。如果很久没更新，确认 `git push` 成功，然后打开仓库页面看文件内容是否已经变化。

### git push 报错

如果是“rejected”或“fetch first”，先执行：

```powershell
git pull --rebase
git push
```

如果提示输入用户名，用户名填 `WLF208`，密码不是登录密码，而是 GitHub Personal Access Token。也可以改用 GitHub Desktop 图形界面登录。

### 不记得怎么打开 PowerShell

在 `作品集` 文件夹里按住 `Shift` 右键，选择“在终端中打开”或“Open PowerShell window here”，再执行 git 命令。
